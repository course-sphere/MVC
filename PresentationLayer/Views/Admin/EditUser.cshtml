@* @model DataAccessLayer.Entities.User

@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Edit User";
}

<div class="max-w-3xl mx-auto bg-white rounded-xl shadow border border-zinc-200 p-6">
    <h2 class="text-xl font-semibold mb-6 flex items-center gap-2">
        <i class="fas fa-user-edit"></i>
        Edit User
    </h2>

    <form asp-action="UpdateUser" method="post" class="space-y-5">
        @Html.AntiForgeryToken()

        <input type="hidden" name="userId" value="@Model.UserId" />

        <!-- Full Name -->
        <div>
            <label class="block text-sm font-medium mb-1">Full Name</label>
            <input name="fullName"
                   value="@Model.FullName"
                   class="w-full px-4 py-2 border rounded-lg focus:ring focus:ring-indigo-200" />
        </div>

        <!-- Email -->
        <div>
            <label class="block text-sm font-medium mb-1">Email</label>
            <input name="email"
                   value="@Model.Email"
                   class="w-full px-4 py-2 border rounded-lg focus:ring focus:ring-indigo-200" />
        </div>

        <!-- Phone -->
        <div>
            <label class="block text-sm font-medium mb-1">Phone Number</label>
            <input name="phoneNumber"
                   value="@Model.PhoneNumber"
                   class="w-full px-4 py-2 border rounded-lg focus:ring focus:ring-indigo-200" />
        </div>

        <!-- Role -->
        <div>
            <label class="block text-sm font-medium mb-1">Role</label>
            <select name="role"
                    class="w-full px-4 py-2 border rounded-lg">
                @foreach (var role in Enum.GetValues(typeof(Role)))
                {
                    <option value="@role"
                            selected="@(Model.Role == (Role)role)">
                        @role
                    </option>
                }
            </select>
        </div>

        <!-- Actions -->
        <div class="flex justify-end gap-3 pt-4">
            <a asp-action="Users"
               class="px-4 py-2 rounded-lg border text-zinc-600 hover:bg-zinc-50">
                Cancel
            </a>

            <button type="submit"
                    class="px-6 py-2 bg-indigo-600 text-white rounded-lg hover:bg-indigo-700">
                Save Changes
            </button>
        </div>
    </form>
</div> *@